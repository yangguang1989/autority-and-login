<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itheima.dao.PermissionDao">
   <!--查询所有-->
    <select id="findAll" resultType="permission">
        select * from sys_permission
    </select>

    <!--查询父权限-->
    <select id="findParentPermission" resultType="permission">
        select * from sys_permission where pid = 0
    </select>

    <!--保存-->
    <insert id="save" parameterType="permission">
        insert into sys_permission values(null,#{permissionName},#{url},#{pid})
    </insert>

    <!--根据角色id查询权限列表-->
    <select id="findPermissionsByRoleId" resultType="permission" parameterType="int">
        select p.* from sys_role_permission rp,sys_permission p where rp.permissionId = p.id and roleId = #{roleId}
    </select>
</mapper>